import Regularcontent from "./regular";

const RegularContent = ({children, mode}) => {
    return(
        <Regularcontent children={children} mode={mode} />
    )
}

export default RegularContent;